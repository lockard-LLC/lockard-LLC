# Branding Kit (Public)

**Owner:** Jerry • **Visibility:** Public

**Wordmark:** PetPass (text logo) + Lockard “+” glyph.  
**Tone:** calm, clear, neutral; avoid DV terminology publicly.  
**Colors (accessible pairs) — pulled from design tokens:**

- Primary gradient: `#4C66FF` → `#95A4FF`, used on pills + hero badges.  
- Secondary gradient: `#34A8FF` → `#86CEFF`, used on outline buttons, icons.  
- Ink: `#F8FAFC` (on-dark body text), `#CBD5E1` (secondary), `#94A3B8` (labels).  
- Success: `#10B981`; Warning: `#F59E0B`; Error: `#EF4444`.

**Surface:** Dark glass backgrounds (`#0B0E14` base, `rgba(12,16,26,0.6)` overlay) with subtle blur and 1px translucent borders.  
**Typography:** Inter (UI), JetBrains Mono (code); 16px base; 1.5 line-height; hero headings follow 48/56 px rhythm.  
**Caller ID label:** “PetPass Care Team”.  
**Assets folder:** `/public/brand/` (logo SVG, favicon, gradient backgrounds).
