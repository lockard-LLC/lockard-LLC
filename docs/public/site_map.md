# File: `docs/public/site_map.md`

**Owner:** Jerry
**Visibility:** Public

## PetPass (petpass.help) — Site Map & Copy Blocks (v0)

* **Home** (`/`) — What PetPass is; benefits to families & clinics; call‑to‑action (contact/partners).
* **How it works** (`/how-it-works`) — intake → voucher → clinic; case‑ID only; what’s covered.
* **Clinics** (`/clinics`) — partner value (discount + slots, net‑15), onboarding steps, contact.
* **Partners** (`/partners`) — community partners; referral workflow; safe comms.
* **Privacy** (`/privacy`) — summary policy; minimal data; retention overview.
* **Terms** (`/terms`) — scope & disclaimers.
* **Accessibility** (`/accessibility`) — WCAG 2.2 AA commitment + contact.
* **Admin portal teaser** (`/control-hub`) — explains that staff tools live at admin.lockard.llc with restricted access; links to public documentation about design philosophy.

### Copy seeds

* **Hero:** “Keep families and pets together. Fast, private vet access coordinated with local clinics—no separation required.”
* **Clinics CTA:** “Join as a partner clinic — steady patients, simple invoicing, net‑15.”
* **Safety note:** “We use **Case IDs only** with clinics. No client names on invoices. Staff work inside a secure hub at admin.lockard.llc.”
